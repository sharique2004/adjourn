# Adjourn dependencies.
#
# Run everything with the drift virtualenv, which already has all of these:
#   "python"
# The system python3 has none of them. This file documents the real dependency
# set so the package can stand on its own later.

falkordb>=1.6.2        # memory_store, Falkor backend
google-genai>=2.16.0   # extraction, Gemini fallback engine
flask>=3.1.3           # board_server
pydantic>=2.0          # schema-locked extraction responses
requests>=2.31         # MeetingScribe GETs, Linear GraphQL, Slack Web API
python-dotenv>=1.0     # config reads adjourn/.env

# Stdlib only, listed so nobody goes looking for a package:
#   smtplib / email.message  -> email_send_executor (Gmail SMTP + app password)
#   sqlite3                  -> memory_store, SQLite fallback backend
#   subprocess               -> gh CLI, claude CLI, `security` (Keychain), osascript
